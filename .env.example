# Light-Tasks API connection
API_BASE_URL=http://app:8000
API_PREFIX=/api/v1

# Admin credentials (used during startup to register webhooks, etc.)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Agent API key — obtain by creating an agent user in Light-Tasks,
# then paste the returned key here.
AGENT_API_KEY=

# Webhook consumer
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=9000
WEBHOOK_CALLBACK_URL=http://agent-network:9000/webhook

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
